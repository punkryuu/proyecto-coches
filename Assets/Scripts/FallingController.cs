using UnityEngine;

public class FallingController : MonoBehaviour
{
    /*private void OnTriggerEnter(Collider other)
    {
        // Si es un NPC
        NPCAgent npc = other.GetComponentInParent<NPCAgent>();
        if (npc != null)
        {
            npc.ResetToLastCheckpoint();
            return;
        }

        // Si es el jugador
        PlayerCar player = other.GetComponentInParent<PlayerCar>();
        if (player != null)
        {
            player.ResetFromFalling();
            return;
        }
    }*/
}
